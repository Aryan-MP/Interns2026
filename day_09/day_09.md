Task : 1
   You Wan't to deploy Virtual Machine with os type
    This Command to run the script = az deployment group create --resource-group <Your_Resource_Group_Name> --template-file template.json --parameters parameters.json --parameters osType=windows 

Task : 2
   You need to Monitor Deployment in the resource group what happen when you run the script.

Task : 3 
   You Need to create two vm in same vnet 
