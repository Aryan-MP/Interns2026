TASK1:CREATING A VIRTUAL MACHINE SCALESETS BY THE GENERALIZED VIRTUAL MACHINE
     1.CREATE A VIRTUAL MACHINE 
     *Resource group
     *images 
     *Vnets and subnets
     *add the packages
    2.capture the image
    3.create the vmscalesets with the generalized vm
TASK2:LOGIN TO WINDOWS WITH CHROME INSTALLATION USING VM
     1.create a script and save its filename.ps1
     2.create a virtual machine and add the custom data and extensions
     3.connect the virtual machine and check if chrome is installed or not
