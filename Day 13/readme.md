Task 1:<br />
To deploy a Vm through arm template and install VS code through CSE and Extensions through Logon tasks.<br />
Conclusion:<br />
A vm was deployed through azuredeploy.json arm template and script.ps1 was deployed through custom script extension which installed vs code and extensions(after restart).<br />