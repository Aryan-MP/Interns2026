A Virtual Machine Scale Set was created and configured to understand scalable and highly available infrastructure in Azure. The virtual machines were launched with custom data, which was used to execute a PowerShell (.ps1) script during deployment. This script automatically installed Google Chrome and IIS on the virtual machines and deployed a static website on the IIS web server.

All assigned tasks for Day 6 were completed successfully. However, due to resource cleanup, all Azure services were deleted after completion, and therefore no screenshots are available. Visual Studio Code extensions were used to maintain images and text together in a single file for better documentation and organization.

All tasks were completed.