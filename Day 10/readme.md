Task 1:<br />
To create a storage account through cloudshell using bicep template.<br />
Conclusion:<br />
A storage account was created by using storage.bicep and deploying through cloudshell.<br />

Task 2:<br />
To create a vnet and one public and one private subnet and deploy a vm in private subnet and install nginx into it which should be accesible by local VM outside of Vnet by using arm template.<br />
Conclusion:<br />
The task was completed by deploying additional vm in public subnet and using reverse proxy concept which forwarded traffic to private vm directly through local network outside of vnet.<br />